{% extends 'base.html.twig' %}

{% block title %}Edit facture{% endblock %}

{% block body %}
    <h1>Edit facture</h1>

    {{ include('factute/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_facture_index') }}">back to list</a>

    {{ include('facture/_delete_form.html.twig') }}
{% endblock %}
